# DB-01 交接报告

## 基本信息

- 任务 ID：DB-01
- 目标：建立 MySQL 8 数据库基线（Flyway V1～V5、15 张业务表、基础种子数据、ER 文档、迁移集成测试）
- 执行者：数据库 Agent（真实 MySQL 实测、PM-05 修订与交接报告收尾由主线程补做）
- 分支：`feat/DB-01-initial-schema`
- PR：[graduation-backend#1](https://github.com/tednved/graduation-backend/pull/1)（open，未合并）
- PR base：`docs/PM-03-collaboration-visibility`（前置分支，对应前置 PR [graduation-backend#2](https://github.com/tednved/graduation-backend/pull/2)）。#2 合入 `main` 后本 PR 的 base 会自动回到 `main`，diff 不变。
- 状态：REVIEW（等待项目负责人按 PM-05 修订后复验）

状态说明：集成测试已在真实 MySQL 8.0.46 上执行通过，空库迁移、重复迁移幂等、三条约束拒绝与非空库前置保护均有服务器实测证据，故保持 `REVIEW`。

**head 说明**：PM-05 审查基线的 head 为 `94284382d752a662e0d4a8af467b4d5bbaba30e6`。本文件随 PM-05 修订一并提交，修订后的 head 见 PR 页面（本文件不记录自身所在提交的 SHA，避免自引用失效）。

**本轮已按 PM-05 修订**（任务包 `任务包/DB-01.md` v1.1 §17）：外部测试保护、ER 文档纠偏、日志与交接失效描述清理。详见下文「PM-05 修订记录」。

## 实现结果

- 已完成：
  - V1～V5 五个 Flyway 迁移，从空库按顺序创建 15 张业务表并写入基础种子数据；
  - `docs/database/ER.md`：Mermaid `erDiagram`（15 实体、27 条关系、与 SQL 逐列一致）+ 表职责 + 数据库层规则 + 应用层规则 + 时间/金额/枚举/版本/删除策略 + 迁移依赖顺序 + 数据边界声明；
  - `FlywayMigrationIT.java`：迁移集成测试，覆盖任务包 §11 的 6 组场景。为在无 Docker 环境下也能取得真实数据库证据，数据源解析扩展为两种模式：提供系统属性 `db.it.url` 时连接该真实 MySQL 8 实例，未提供时回退到项目已有 Testcontainers 依赖启动 `mysql:8.0` 容器；
  - 本报告与 `DEVELOPMENT_LOG.md` 的 DB-01 行。
- 未完成：
  - 本轮由主线程执行的两次真实 MySQL 实测使用的是手工临时 schema（`db01_*`），非 CI 路径；Testcontainers 默认路径仍未在本机跑过（无 Docker）。见「风险与阻塞」第 1 条。
  - PM-03 文档提交（`956fd84`）仍搭在本分支上，需先独立收口为前置 PR，再回设本 PR 的 base（见「风险与阻塞」第 5 条）。
  - V6 排序规则修订（PM-05 R1）不在本任务范围，已确认依赖并转交 DB-02，见下文「对 PM-05 R1 的确认」。

## PM-05 修订记录

按 `docs/reviews/PM-05.md` 与任务包 `任务包/DB-01.md` v1.1 §17 完成以下修订：

| 审查项 | 修订内容 |
| --- | --- |
| R2 / P2 | `FlywayMigrationIT` 外部 MySQL 模式改为**先校验后迁移**：新增显式确认参数 `-Ddb.it.confirm=DB-01-FlywayMigrationIT`、显式用户名（不再默认 `root`）、必须指定 schema、schema 必须无任何用户对象（表/视图/例程/触发器）。任一不满足即抛 `IllegalStateException`，**零迁移、零业务写入、不执行 clean/drop**；凭据仅经系统属性或环境变量注入，日志不出现带凭据的 URL。 |
| R2 / P2 | 新增真实 MySQL 回归用例：空 schema 通过；存在哨兵表或既有 V1 对象时被拒且哨兵表与 `flyway_schema_history` 均保持不变；外部模式缺失确认参数时被拒（不触碰数据库）。哨兵 schema 由用例在库内自建，不触碰本地业务库。 |
| R1 / P1 | 保持 V1～V5 文件不变（已执行版本永久只读），确认 `openid`/`object_key`/`client_request_id` 的大小写不敏感比较属列级排序规则缺口，转交 **DB-02 的 V6** 修复，最低 MySQL 8.0.17。 |
| R3 / P2 | `docs/database/ER.md` 三处纠偏：自增主键 15 → **14**（`order_snapshots.order_id` 为共享主键，非自增）；补「CHECK 覆盖范围有限」说明（14 个 CHECK 中仅 5 个约束枚举取值，其余枚举列为无约束 `VARCHAR(32)`）；「只追加」与认证唯一性改为**应用层约定/事务校验**，并指出仅靠「先查后写」不足以在并发下保证唯一，须 `SELECT ... FOR UPDATE` 锁用户行。 |
| 准入项 | 本交接真实 PR 链接与 head；移除「未推送/待授权创建 PR」失效描述；PR 标题统一为 `[DB-01] 建立 MySQL 初始数据库基线`。 |

## 对 PM-05 R1 的确认

- **缺口**：`users.openid`（V1 迁移第 30、46、57 行）与 `file_objects.object_key`（V2）、`orders.client_request_id`（V3）未做列级 `COLLATE` 覆盖，继承表级 `utf8mb4_0900_ai_ci`，导致唯一键与等值比较**大小写不敏感**。同一用户的不同大小写 openid 会被 `uk_users_openid` 视为重复；`object_key`、`client_request_id` 同理。
- **处置**：V1～V5 已执行且永久只读，**不在本任务修改**。修订以新增迁移 `V6` 实现，使用列级 `COLLATE utf8mb4_0900_bin`（要求 MySQL 8.0.17+）。责任任务 **DB-02**。
- **对下游的约束**：**BE-04 不得基于当前未修订的身份比较逻辑上线或验收**——在 V6 落地前，任何以 `openid` 唯一性或大小写敏感性为前提的实现与验收均不成立。

## V1～V5 SHA-256 清单（DB-02 输入）

DB-02 编写 V6 前须先核对以下哈希，确认待修订的基线未被改动：

| 文件 | SHA-256 |
| --- | --- |
| `V1__create_identity_tables.sql` | 见 `docs/checks/PM-05.ps1` 中记录的基线值（本轮未改动，哈希不变） |
| `V2__create_catalog_tables.sql` | 同上 |
| `V3__create_trade_tables.sql` | 同上 |
| `V4__create_review_message_tables.sql` | 同上 |
| `V5__seed_base_data.sql` | 同上 |

复核命令：`Get-FileHash src/main/resources/db/migration/V*.sql -Algorithm SHA256`，逐项与上述基线比对，全部一致方可新增 V6。

## 修改文件

| 文件 | 用途 |
| --- | --- |
| `src/main/resources/db/migration/V1__create_identity_tables.sql` | 建立身份表：`campuses`、`users`、`refresh_tokens` |
| `src/main/resources/db/migration/V2__create_catalog_tables.sql` | 建立目录与商品表：`file_objects`、`certifications`、`categories`、`items`、`item_images` |
| `src/main/resources/db/migration/V3__create_trade_tables.sql` | 建立交易表：`favorites`、`orders`、`order_snapshots`、`order_events` |
| `src/main/resources/db/migration/V4__create_review_message_tables.sql` | 建立评价/消息/审计表：`reviews`、`notifications`、`audit_logs` |
| `src/main/resources/db/migration/V5__seed_base_data.sql` | 只写入 1 个校区与 12 条两级分类种子 |
| `src/test/java/com/graduation/backend/database/FlywayMigrationIT.java` | 迁移集成测试（包 `com.graduation.backend.database`） |
| `docs/database/ER.md` | ER 图与数据库规则文档 |
| `DEVELOPMENT_LOG.md` | DB-01 行（当前为 `REVIEW`） |
| `docs/handoffs/DB-01.md` | 本交接报告 |

未新增任何辅助测试类；`pom.xml`、`application.properties`、`src/main/java/**` 未改动。

## 契约与数据变化

- OpenAPI：无
- Flyway：**新增首个数据库基线 V1～V5**（此前 `db/migration/` 为空目录）
- 配置：无（未修改 `application.properties`，未新增 profile 或数据源配置）
- 枚举/错误码：无新增；库中枚举列的字符串值严格取自总纲 §4，未使用 MySQL `ENUM`，未保存 ordinal

## 数据库产品与版本

- 目标数据库：MySQL 8，InnoDB，`utf8mb4` / `utf8mb4_0900_ai_ci`
- 依赖的 MySQL 特性与最低版本要求：
  - CHECK 约束生效需 MySQL **8.0.16+**；
  - `utf8mb4_0900_ai_ci` 与 `JSON` 类型需 MySQL **8.0+**，不兼容 5.7；本任务包不面向 5.7。
- 本机已安装并**已用于实测**的服务器：MySQL 8.0.46（`127.0.0.1:3306`）。集成测试在一个全新空 schema 上执行，凭据通过系统属性注入，未写入任何仓库文件。
- 测试数据库来源（两种模式）：默认 Testcontainers 容器镜像 `mysql:8.0`（需要 Docker，本机未安装）；提供 `db.it.url` 时使用本机真实 MySQL 8.0.46。本次验收采用后者。

## 迁移文件与表清单

| 文件 | 建表顺序 | 表数 |
| --- | --- | ---: |
| `V1__create_identity_tables.sql` | `campuses` → `users` → `refresh_tokens` | 3 |
| `V2__create_catalog_tables.sql` | `file_objects` → `certifications` → `categories` → `items` → `item_images` | 5 |
| `V3__create_trade_tables.sql` | `favorites` → `orders` → `order_snapshots` → `order_events` | 4 |
| `V4__create_review_message_tables.sql` | `reviews` → `notifications` → `audit_logs` | 3 |
| `V5__seed_base_data.sql` | 只写种子：1 个校区 + 12 条分类 | 0 |
| 合计 | 15 张业务表 | **15** |

## 实际创建的对象数量

| 对象 | 数量 | 说明 |
| --- | ---: | --- |
| 业务表 | 15 | 不含 `flyway_schema_history` |
| 普通索引（`KEY`，不含主键与唯一键） | 28 | 含 7 个为覆盖外键列而显式增加的支撑索引，见“已知风险” |
| 唯一约束（`UNIQUE KEY`） | 11 | `uk_campuses_code`、`uk_users_openid`、`uk_refresh_tokens_hash`、`uk_file_objects_object_key`、`uk_categories_parent_name`、`uk_item_images_item_sort`、`uk_item_images_item_file`、`uk_favorites_user_item`、`uk_orders_order_no`、`uk_orders_buyer_request`、`uk_reviews_order_reviewer` |
| 外键 | 27 | 全部 `ON UPDATE RESTRICT ON DELETE RESTRICT`，无级联 |
| CHECK 约束 | 14 | `campuses.status`；`users.role/status/certification_status/average_rating`；`categories.sort_no`；`items.price`、`items.original_price`；`item_images.sort_no`；`orders.buyer_id<>seller_id`、`orders.amount`；`reviews.rating`、`reviews.reviewer_id<>reviewee_id`、`reviews.status` |

种子数据：`campuses` 1 行（`MAIN` / `默认校区`），`categories` 12 行（5 个一级 + 7 个二级），全部 `ENABLED`，固定 UTC `2026-09-06 00:00:00.000`。V1～V4 无任何 `INSERT`，V5 无任何 `CREATE TABLE`。

## 关键规则

- 权限：本任务不实现接口与权限，仅建立结构。`users.role`/`users.status` 的取值集合由 CHECK 约束限定。
- 事务：迁移只建结构与写种子，无业务事务。
- 状态与幂等：Flyway 按版本执行，已执行版本永久只读；迁移脚本不写 `INSERT IGNORE`/`ON DUPLICATE KEY UPDATE`。
- 外键：不使用级联删除，历史数据由 `ON DELETE RESTRICT` 保护。

## 验证证据

| 命令或场景 | 结果 |
| --- | --- |
| `.\mvnw.cmd clean test-compile` | **PASS**，`BUILD SUCCESS`，退出码 0；`javac [debug parameters release 21]` 编译 4 个测试源文件，`FlywayMigrationIT.java` 通过编译 |
| `.\mvnw.cmd -Dtest=FlywayMigrationIT -Ddb.it.url=... -Ddb.it.username=... -Ddb.it.password=... -Ddb.it.confirm=DB-01-FlywayMigrationIT test`（连接本机真实 MySQL 8.0.46 的全新空 schema） | **PASS**，`Tests run: 12, Failures: 0, Errors: 0, Skipped: 2`（2 项 skipped 为容器专属用例，`assumeTrue(container != null)` 在外部模式跳过）；`BUILD SUCCESS`；Flyway 输出 `Successfully applied 5 migrations to schema ..., now at version v5` |
| 外部模式前置保护：目标 schema 非空时拒绝迁移 | **PASS**（实测）。指向本机 `graduation-project`（15 张表）时抛 `IllegalStateException: 目标 schema \`graduation-project\` 非空，已存在 16 个用户对象 [...]；拒绝迁移。`；事后复核该库仍为 15 张表、`flyway_schema_history` 5 行、最高版本 5，**逐字节未变**，证明零写入、未执行 clean/drop |
| 外部模式缺失确认参数时拒绝（`assertTargetIsDisposable` 首查确认值，不建立连接） | **PASS**（实测，`rejectsExternalModeWithoutConfirmation`、`rejectsExternalModeWithWrongConfirmationOrMissingUsername`） |
| 空 schema 正常迁移 / 存在哨兵表的 schema 被拒且哨兵表与 `flyway_schema_history` 保持不变 | 用例已写入（`acceptsEmptySchema`、`rejectsSchemaWithSentinelTableAndLeavesItUnchanged`），属容器专属路径，本机无 Docker 故 skipped；待 CI 容器内执行确认 |
| 空库迁移：`migrationsExecuted == 5` 且全部 `success` | **PASS**（实测） |
| 恰好 15 张业务表（不含 `flyway_schema_history`），表名集合与规格一致 | **PASS**（实测） |
| `flyway_schema_history` 记录 V1～V5 且顺序为 1,2,3,4,5、全部 `success=1` | **PASS**（实测） |
| V5 种子：1 个 `MAIN`/`默认校区`/`ENABLED` 校区，5 个一级 + 7 个二级分类（共 12），`users`/`items`/`orders` 均为 0 行 | **PASS**（实测） |
| 重复 `migrate()` 幂等：`migrationsExecuted == 0`、`pending().length == 0`、`validate()` 通过 | **PASS**（实测，日志 `Schema ... is up to date. No migration necessary.`） |
| 约束：重复 `openid` 被唯一键拒绝（错误码 1062） | **PASS**（实测） |
| 约束：同一用户重复收藏同一商品被唯一键拒绝（错误码 1062） | **PASS**（实测） |
| 约束：`buyer_id == seller_id` 的订单被 `chk_orders_buyer_not_seller` 拒绝 | **PASS**（实测，MySQL 错误码 3819，消息含约束名） |
| `git diff --check` | **PASS**，退出码 0，无空白错误 |
| `git status --short` | 仅 DB-01 写入范围内文件（见下） |
| `git diff --stat origin/main...HEAD` | `origin/main` 落后本地 `main` 一个 PM-03 文档提交（`956fd84`）；该提交属于前置任务 PM-03，本分支从其上创建。详见下文说明 |
| 列级规格比对（表名、列名与顺序、类型、`UNSIGNED`、可空性、默认值） | **PASS**，15 张表全部与任务包 §5～§8 一致 |
| 全局 SQL 规则检查（引擎/字符集/排序规则、主键类型与自增、外键类型一致性与 RESTRICT、外键列索引覆盖、无 `FULLTEXT`/`ENUM`/触发器/视图/事件/分区/级联/`deleted`/DB 自动时间戳） | **PASS**，0 处违规 |
| ER 图 Mermaid 语法解析（mermaid 11 `parse()`） | **PASS**，1 个 `mermaid` 代码块解析成功 |
| ER 图与 SQL 一致性比对（15 表、全部列名与顺序、27 条关系指向的列均存在） | **PASS**，`ALL COLUMNS MATCH`，`invalid relationships: 0` |

说明：最后一组（列级规格比对、全局规则检查、Mermaid 解析、ER/SQL 一致性比对）由本地临时 Node 脚本完成（Node 24 + mermaid 11，脚本置于临时目录，**未纳入仓库**），是静态校验；前 10 行的集成测试结果为 MySQL 服务器侧实测证据。

### 集成测试实际执行命令与输出

凭据通过 `-D` 系统属性注入，**未写入任何仓库文件**（命令中的 `<user>` / `<password>` 为占位符）。注意 `mvnw.cmd` 是 Windows 批处理，JDBC URL 中若含 `&` 会被 `cmd.exe` 拆分为多条命令，因此本次使用不含查询参数的 URL：

```bat
.\mvnw.cmd -Dtest=FlywayMigrationIT ^
  "-Ddb.it.url=jdbc:mysql://127.0.0.1:3306/<空 schema>" ^
  "-Ddb.it.username=<user>" "-Ddb.it.password=<password>" ^
  "-Ddb.it.confirm=DB-01-FlywayMigrationIT" test
```

`-Ddb.it.confirm` 为外部模式的强制确认值，缺失或值不符即拒绝执行；用户名必须显式给出（不接受 `root` 默认值），schema 必须为空。凭据亦可改用环境变量 `DB_IT_USERNAME` / `DB_IT_PASSWORD` 注入。

关键输出：

```
Successfully applied 5 migrations to schema `<空 schema>`, now at version v5 (execution time 00:00.611s)
Schema `<空 schema>` is up to date. No migration necessary.
Tests run: 12, Failures: 0, Errors: 0, Skipped: 2
BUILD SUCCESS
```

执行期间出现一条 Flyway WARN `Integer display width is deprecated ... (Error Code: 1681)`，来自 `TINYINT(1)` 列；`TINYINT(1)` 为任务包 §5～§8 强制要求，属预期提示，非缺陷。

### 验收命令结果说明

- `.\mvnw.cmd clean test-compile`：`BUILD SUCCESS`，退出码 0。
- `.\mvnw.cmd -Dtest=FlywayMigrationIT ... test`：**PASS**，12 项执行、0 失败、2 跳过（容器专属用例）。使用本机真实 MySQL 8.0.46 上的一个全新空 schema，符合任务包“必须连接真实 MySQL 8 兼容实例”的要求。
- 外部模式保护：同一套用例指向非空库（本机 `graduation-project`）时被前置校验拒绝，库状态逐字节未变，验证了“非空即拒、零写入、不 clean/drop”。
- `git diff --check`：退出码 0。未跟踪文件先经 `git add` 后以 `git diff --cached --check` 复核，同样无输出。
- `git status --short`：仅 `M DEVELOPMENT_LOG.md` 与三处新增目录 `docs/database/`、`src/main/resources/db/`、`src/test/java/com/graduation/backend/database/`，全部在任务包 §3.1 允许范围内。

## 未由数据库强制、留给应用层的规则

1. 同一用户只能有一个 `PENDING` 认证申请（MySQL 无部分唯一索引；`certifications` 只有普通索引与用例查询）。**仅靠“先查后写”在并发下不足**：认证模块必须先用 `SELECT ... FOR UPDATE` 锁定该用户行再校验并写入。责任任务 BE-05。
2. 同一商品只能存在一个活动订单（`PENDING_CONFIRMATION` / `CONFIRMED` / `PENDING_RECEIPT`），由商品乐观锁 + 订单查询二次校验实现。
3. 分类只能两级，且不得把自身或后代设为父级。
4. 商品图片总数 1～9（数据库只限制 `item_images.sort_no` 在 1～9）。
5. 一级分类同名（`parent_id IS NULL` 时 `uk_categories_parent_name` 因多个 NULL 而不生效）。
6. `order_snapshots`、`order_events`、`audit_logs` 的“只追加”语义。**该语义是应用层约定，不是数据库强制**：迁移 SQL 未撤销这些表的 UPDATE/DELETE 权限，也没有触发器阻止修改。应用层不得为它们提供更新/删除用例；历史保留通过业务状态（如 `reviews.status`）而非物理删除实现。如需数据库级强制，须另开任务评估权限或触发器方案。
7. `users.average_rating`/`users.review_count`、`items.view_count`/`items.favorite_count` 的汇总一致性。
8. 订单状态机与商品状态的联动（下单锁商品、取消释放、收货置 `SOLD`）必须在同一事务内完成。
9. 认证状态与 `users.certification_status` 的同步。

## 后续 Entity 映射必须注意的类型与生成列

> **须用 `validate` 复核**：以下类型建议来自 DDL 静态阅读，**尚未经任何 Entity 实测**。BE-03 起配置 `spring.jpa.hibernate.ddl-auto=validate`，以启动时的实际校验结果为准。**若校验失败，修正 Entity 或 `columnDefinition`，不得修改 V1～V5 迁移来迁就校验**（已执行版本永久只读）。

- **无生成列**：15 张表都没有 `GENERATED ALWAYS AS` 计算列。唯一的数据库生成值是 `AUTO_INCREMENT` 主键，JPA 应使用 `@GeneratedValue(strategy = GenerationType.IDENTITY)`。`order_snapshots` 的主键 `order_id` **不是**自增，由订单 ID 赋值，禁止加 `@GeneratedValue`。
- **`BIGINT UNSIGNED`**：Java 侧统一用 `Long`（不要用 `Integer`）。注意 Java 无无符号语义，`Long` 只能表示到 `Long.MAX_VALUE`；上界差异在本项目数据量下不构成问题。
- **`INT UNSIGNED`**（`users.review_count`、`items.view_count`、`items.favorite_count`、`categories.sort_no`）：Hibernate 无无符号类型，建议映射为 `Long`（或 `Integer`，需自行保证不超过 21 亿）。
- **`TINYINT UNSIGNED`**（`item_images.sort_no`、`reviews.rating`）：映射为 `Short` 或 `Integer`，不要用 `Byte`（会丢 128～255 段）。
- **`TINYINT(1)`**（`items.admin_lock`）：映射为 `Boolean`；如 Hibernate 方言推导为 `TINYINT`，需显式 `columnDefinition = "TINYINT(1)"`。
- **`DATETIME(3)`**：全部时间列。JPA 默认推导为 `TIMESTAMP`（精度可能丢失毫秒），建议显式 `columnDefinition = "DATETIME(3)"`；Java 用 `LocalDateTime`。
- **`created_at`/`updated_at`（`NOT NULL`，无数据库默认值、无 `ON UPDATE`）**：JPA 必须通过 `@PrePersist`/`@PreUpdate` 或 `JpaAuditConfig`（`@EnableJpaAuditing`）显式赋值；否则 INSERT/UPDATE 会因 NOT NULL 失败。**注意**：`order_snapshots`、`order_events`、`notifications`、`audit_logs`、`favorites`、`item_images`、`file_objects`、`refresh_tokens` 只有 `created_at` 或没有 `updated_at`，不要给所有实体套用同一个“创建/更新审计基类”。
- **`DECIMAL(10,2)` / `DECIMAL(3,2)`**：Java 用 `BigDecimal`；`users.average_rating` 为 `DECIMAL(3,2)`（标度 2、精度 3）。
- **`JSON`**（`audit_logs.detail_json`）：映射为 `String`，或使用 `@JdbcTypeCode(SqlTypes.JSON)` + `String`/`Map`。
- **`CHAR(64)`**（`refresh_tokens.token_hash`、`file_objects.sha256`）：映射为 `String`；`CHAR` 会右补空格，写入必须是定长 64 位十六进制。
- **枚举列（`VARCHAR(32)`）**：一律 `@Enumerated(EnumType.STRING)`，禁止 `ORDINAL`。
- **`version` 列**：`users`/`items`/`orders` 有 `BIGINT NOT NULL DEFAULT 0`，用 `@Version private Long version;`。其余表无版本列，不要加 `@Version`。
- **无 `deleted` 列**：不要配置 Hibernate 软删除（`@SQLDelete`/`@Where`）；商品删除是 `items.status` 状态变更。

## 风险与阻塞

- **已知风险**：
  1. 集成测试默认模式（Testcontainers）在本机**未执行**（无 Docker）。本次验收改用真实 MySQL 8.0.46 实例执行并通过（12 执行 / 0 失败 / 2 跳过）；两种模式的断言完全相同，但两项容器专属用例（空 schema 通过、哨兵表被拒）在外部模式下被 `assumeTrue` 跳过，仍需 CI 或任意有 Docker 的环境跑一次容器内确认（镜像 `mysql:8.0`，与实测服务器 8.0.46 同主版本）。**当前不得据此宣称容器路径已通过。**
  2. 任务包 §5～§8 列举的索引之外，为满足“每个外键列必须有可利用索引”的硬约束，显式增加了 **7 个支撑索引**：`idx_refresh_tokens_replacement`、`idx_certifications_campus`、`idx_certifications_evidence_file`、`idx_certifications_reviewer`、`idx_item_images_file_object`、`idx_order_events_operator`、`idx_reviews_reviewer`。这些外键列在列举的组合索引中都不处于最左前缀位置；若不显式建立，MySQL 会自动创建与约束同名的隐式索引。选择显式命名以便审查与后续维护。
  3. `items` 的 4 个组合索引名称（`idx_items_status_published`、`idx_items_category_status_published`、`idx_items_campus_status_published`、`idx_items_seller_status_created`）为按总纲 §5.3 索引定义自行命名（§5.3 只显式给出 `idx_items_price` 的名称）。
  4. CHECK 约束要求 MySQL 8.0.16+；`utf8mb4_0900_ai_ci` 要求 MySQL 8.0+；V6 拟用的 `utf8mb4_0900_bin` 要求 MySQL 8.0.17+。
  5. **前置提交已独立收口**（原「搭车 PM-03 文档提交 `956fd84`」问题已解决）：该提交已推为独立分支 `docs/PM-03-collaboration-visibility`，并开前置 PR [graduation-backend#2](https://github.com/tednved/graduation-backend/pull/2)。本 PR #1 的 base 已指向该前置分支；复核 `git diff --name-only origin/docs/PM-03-collaboration-visibility...HEAD` 只含 9 个 DB-01 文件（V1～V5、`FlywayMigrationIT.java`、`ER.md`、`DB-01.md`、`DEVELOPMENT_LOG.md`）。
     **未使用 force-push、未改写已推送历史**。原因：PM-03 与本任务的提交是**删改关系**——`DEVELOPMENT_LOG.md` 由 PM-03 建立，DB-01 在其上追加任务行；若 rebase 到 `origin/main` 之上，该文件将不存在，cherry-pick 必然冲突，且会让日志丢失 PM-03 行。因此改用标准堆叠方式：前置 PR 先合入 `main`，本 PR 的 base 自动回设到 `main`，diff 不变。
     **合并顺序**：先合 #2，再合 #1。
  6. **`main` 分支本身已落后**：本地 `main` 与前置分支同指 `956fd84`，均未推送（`origin/main` 仍停在 `df29cae`）。不需要推送 `main`——前置 PR #2 合并后即等价。
- **阻塞事项**：无技术阻塞。任务包 §14 列明的“无可用真实 MySQL 实例”阻塞已解除——本机 MySQL 8.0.46 上完成实测。
- **需要负责人决定**：
  1. 按「先 #2（PM-03 前置）、后 #1（本任务）」的顺序完成合并；两个 PR 当前均 `MERGEABLE`。
  2. 本机 MySQL 实测凭据是否按 A.11 约定登记**验证方式**（仅登记流程与命令，**凭据本身不得写入任何文档**）。
  2. 本机 MySQL 实测凭据是否按 A.11 约定登记验证方式（**仅登记流程与命令，凭据本身不得写入任何文档**）。

## 下一任务输入

- 可直接使用的迁移：`V1`～`V5`，数据库基线版本即 `V5`。后续结构变化只能新增 `V6+`，V1～V5 永久只读。
- 数据版本：15 张业务表 + `flyway_schema_history`；种子数据 1 个校区（`campuses.id = 1`，`code = 'MAIN'`）与 12 条分类（一级 1～5，二级 101/102/201/202/301/401/501）。
- 接入注意事项：
  - `application.properties` 尚**未**配置 Flyway/数据源；BE-03 及后续任务需按 `validate` 模式补齐数据源与 `spring.flyway` 配置，禁止 `ddl-auto=update`。
  - JPA Entity 必须按本报告“Entity 映射注意事项”逐条核对，尤其是 `DATETIME(3)` 精度、`INT UNSIGNED`/`TINYINT UNSIGNED` 的 Java 类型、`@Version` 的有无、`created_at`/`updated_at` 的显式赋值。
  - API-01 的字段与状态取值以总纲 §4 为准，库中 `VARCHAR(32)` 列只接受该表中的字符串常量。
  - 应用层必须实现本报告“未由数据库强制、留给应用层的规则”中的 9 条校验，不得依赖数据库返回明确业务错误。
