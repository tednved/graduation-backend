# KNOWN_ISSUES

非阻断问题清单。这里只记录不影响 10 分钟答辩演示、且已有绕行方案的问题；
P0/P1 一律在当轮任务中修复，不进本文件。

## KI-01 认证证据的不可泄露无法在公开 API 层面证伪（P2）

- 现象：QA-01 的不变量「认证证据不会通过公开媒体接口泄露」无法用公开请求验证。
- 原因：认证证据（`realName`/`studentNo`）只有提交时的写入路径，没有对应的读取接口；上传接口返回的是受控的 `/media/{fileId}` 地址，不暴露存储路径。缺一个「证据文件」入口，也就无法构造一次越权读取来证伪。
- 绕行：公开侧只能确认接口一律返回脱敏值（`张*`、`20******34`）；已由 `CoreApiFlowTests.certificationApproveFlowSyncsUserStatus` 覆盖。演示不涉及。
- 关闭条件：出现认证证据的独立文件或下载入口时，必须补一条越权读取用例。

## KI-02 契约内没有管理端订单列表（P2）

- 现象：管理员看不到与自己无关的订单，管理台无法做订单维度的排查。
- 原因：`/orders/{id}` 已收紧为「只认参与方」（管理员本身也可以是买家或卖家，按角色放行等于让任何管理员读到无关订单的金额、双方与时间线）；契约里没有替代的管理端全量订单接口。
- 绕行：需要排查时直接查库；答辩演示不涉及管理端订单页。
- 关闭条件：新增管理端订单查询接口时一并补审计与分页约定。

## KI-03 管理员不能审核自己的认证（P2，行为变更）

- 现象：提权后的管理员无法通过自己的认证申请，申请会一直停在 `PENDING`。
- 原因：与「管理员不能禁用自己的账号」对齐，审核权不能用来给自己发认证（通过与驳回双向封禁）。
- 绕行：项目本来就没有种子管理员，环境搭建走的是直接改库置 `certification_status='APPROVED'`；演示前按这条路径准备认证态即可。
- 关闭条件：不需要关闭——这是有意的约束，记录在此仅用于避免演示前踩坑。
